<UserControl x:Class="Hyaku.Views.InputView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeSmall}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="150" d:DesignWidth="440">

    <Grid x:Name="ButtonsGrid" Background="{StaticResource PhoneChromeBrush}">
        <StackPanel Name="OuterPanel" Orientation="Vertical">
            <StackPanel Name="TopRow" Orientation="Horizontal">
                <TextBlock Name="NextNumber0">
                </TextBlock>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
